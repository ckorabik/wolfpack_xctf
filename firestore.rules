rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Coach authorization and attendance writes are handled only by callable
    // Cloud Functions. Browser clients have no direct database access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
